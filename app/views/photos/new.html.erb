<div class="album-wrapper">
  
  <h2 style="text-align: center; margin-bottom: 20px; color: #333;">📸 新規アルバム作成</h2>

  <div class="post-card" style="margin: 0 auto;">
    
    <%= form_with model: @photo, local: true, html: { style: "display: flex; width: 100%; gap: 40px;" } do |form| %>
      
      <% if @photo.errors.any? %>
        <div style="position: absolute; top: 10px; left: 10px; color: red;">入力にエラーがあります</div>
      <% end %>

      <div class="post-left">
        <h4 style="margin-top: 0; color: #555;">アルバムプレビュー</h4>
        
        <label for="photo_image" class="post-preview-label">
          <div id="preview-placeholder">
            <span style="font-size: 3em;">📷</span><br>
            <strong>画像をアップロード</strong><br>
            <span style="font-size: 0.8em;">または、ここをクリックして画像を選択</span>
          </div>
          <img id="image-preview" style="width: 100%; height: 100%; object-fit: cover; display: none; position: absolute; top: 0; left: 0;" />
        </label>
        
        <%= form.file_field :image, id: "photo_image", accept: 'image/jpg,image/jpeg,image/png', style: "display: none;", onchange: "previewImage(event)" %>
      </div>

      <div class="post-right">
        
        <div class="auth-form-group">
          <%= form.label :content, "アルバム名 / 説明", class: "auth-label" %>
          <%= form.text_area :content, class: "auth-input", placeholder: "例：2024年 卒業式", rows: 2 %>
        </div>

        <div class="form-row">
          <div class="form-col">
            <%= form.label :event_date, "出来事の日付", class: "auth-label" %>
            <%= form.date_field :event_date, class: "auth-input" %>
          </div>
          <div class="form-col">
            <label class="auth-label">投稿者</label>
            <div class="auth-input" style="background: #eee; color: #888;">
               <%= current_user.nickname.split('@').first %>
            </div>
          </div>
        </div>

        <div class="auth-form-group">
          <label class="auth-label">テーマカラー（※準備中）</label>
          <div>
            <span class="color-circle" style="background: #eb8428; border: 2px solid #333;"></span>
            <span class="color-circle" style="background: #4caf50;"></span>
            <span class="color-circle" style="background: #2196f3;"></span>
            <span class="color-circle" style="background: #9c27b0;"></span>
            <span class="color-circle" style="background: #f44336;"></span>
          </div>
        </div>

        <div class="form-actions" style="margin-top: auto;">
          <%= link_to "キャンセル", photos_path, class: "btn-cancel" %>
          <%= form.submit "作成", class: "btn-submit" %>
        </div>

      </div>
    <% end %>

  </div>
</div>

<script>
  function previewImage(event) {
    const reader = new FileReader();
    reader.onload = function() {
      const output = document.getElementById('image-preview');
      output.src = reader.result;
      output.style.display = 'block'; // 画像を表示
      document.getElementById('preview-placeholder').style.display = 'none'; // 文字を消す
    }
    // 選ばれたファイルを読み込む
    if(event.target.files[0]) {
      reader.readAsDataURL(event.target.files[0]);
    }
  }
</script>